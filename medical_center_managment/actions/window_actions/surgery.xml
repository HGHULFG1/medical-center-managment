<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- surgery action window -->
    <record id="medical_center_managment.surgery_patient" model="ir.actions.act_window">
        <field name="name">Surgeries</field>
        <field name="res_model">surgery</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('patient_id','=',active_id)]</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                <!-- Add Text Here -->
            </p><p>
                <!-- More details about what a user can do with this object will be OK --> 
            </p>
        </field>
    </record>


    <record id="medical_center_managment.surgery_doctor" model="ir.actions.act_window">
        <field name="name">Surgeries</field>
        <field name="res_model">surgery</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('doctor_id','=',active_id)]</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                <!-- Add Text Here -->
            </p><p>
                <!-- More details about what a user can do with this object will be OK --> 
            </p>
        </field>
    </record>

</odoo>
